name: Publish to GHCR

on:
  push:
    branches: ['main']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Login to GitHub Docker Repo
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u lavylavenderr --password-stdin

      - name: Build Docker Image
        run: docker build -t temporaryweb .

      - name: Tag Docker Image
        run: |
          docker tag temporaryweb ghcr.io/lavylavenderr/temporaryweb:latest
          docker tag temporaryweb ghcr.io/lavylavenderr/temporaryweb:${{ github.sha }}

      - name: Push Docker Images
        run: |
          docker push ghcr.io/lavylavenderr/temporaryweb:latest
          docker push ghcr.io/lavylavenderr/temporaryweb:${{ github.sha }}
